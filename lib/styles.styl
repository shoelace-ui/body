/**
 * Module dependencies
 */

@require 'shoelace-ui-font'
@require 'shoelace-ui-gray'
@require './variables'

/**
 * Apply `body` default styles.
 */

body
  font base
  margin 0
  color gray--darkest
  if body--bg isnt inherit
    background body--bg
